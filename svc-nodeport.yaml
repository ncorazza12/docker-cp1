apiVersion: v1
kind: Service
metadata:
  name: store-api-np
  namespace: store-dev
spec:
  type: NodePort
  selector:
    app: store-api
  ports:
    - port: 80
      targetPort: http
      nodePort: 30081